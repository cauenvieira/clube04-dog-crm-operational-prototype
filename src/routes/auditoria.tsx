import { createFileRoute } from "@tanstack/react-router";
import { PageHeader, MockBadge } from "@/components/layout/AppLayout";
import { useAudit } from "@/lib/audit-store";
import { formatDateTime } from "@/lib/mock-data";
export const Route = createFileRoute("/auditoria")({ head: () => ({ meta: [{ title: "Auditoria · CRM Clube04" }] }), component: Auditoria });
function Auditoria(){const{entries}=useAudit();return <><PageHeader title="Auditoria" subtitle="Registro local das ações feitas no protótipo operacional." badge={<MockBadge/>}/><div className="px-8 py-6"><div className="rounded-xl border border-border bg-card overflow-hidden"><table className="w-full text-sm"><thead className="bg-muted/40 text-[11px] uppercase tracking-wider text-muted-foreground"><tr><th className="px-3 py-2.5 text-left">Data</th><th className="px-3 py-2.5 text-left">Ação</th><th className="px-3 py-2.5 text-left">Usuário</th><th className="px-3 py-2.5 text-left">Lead</th><th className="px-3 py-2.5 text-left">Resumo</th></tr></thead><tbody>{entries.length===0&&<tr><td colSpan={5} className="px-3 py-8 text-center text-muted-foreground">Nenhuma ação registrada ainda.</td></tr>}{entries.map(e=><tr key={e.id} className="border-t border-border align-top"><td className="px-3 py-2.5 whitespace-nowrap text-muted-foreground">{formatDateTime(e.at)}</td><td className="px-3 py-2.5 font-medium">{e.action}</td><td className="px-3 py-2.5"><div>{e.actorName}</div><div className="text-xs text-muted-foreground">{e.actorRole}</div></td><td className="px-3 py-2.5 text-muted-foreground">{e.leadName??"—"}</td><td className="px-3 py-2.5">{e.summary}</td></tr>)}</tbody></table></div></div></>}
